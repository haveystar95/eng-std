# Device-batch — прогон 1 (день 1)

Приёмочный прогон приложения на iPhone с чистого аккаунта.
Аккаунт: **denys.solonyna@yalantis.net** (чистый). Backend: **backend2** через ngrok.
Контроль состояния: БД `wordtrainer` (wt_db :5433), логи backend2 (wt_app), `flutter run`.

Формат: N. Действие → Ожидаемо на экране → Проверка (SQL/лог). Расхождение → findings, не чиним по ходу (кроме блокеров).

---

## Чек-лист блоков

| Блок | Тема | Статус |
|---|---|---|
| 0 | Преполётный чек | ✅ PASS |
| 1 | Регистрация + онбординг + язык (ru) + профиль | 🟩 почти (онбординг+профиль ✅; ru→блок 8) |
| 2 | Первый разбор — воспроизведение бага (генерация ~20 → триаж «не знаю» → тренировка → кап сессии) | ✅ F8 воспроизведён → fixed in run → device-verified |
| 3 | Тренажёры: multiple_choice / word_bank / typing (+«Не помню»), фидбек, автопроизношение, итог | 🟩 MC+word_bank+фидбек+итог ✅; **typing не достаётся в окно д1/д2** (review 5+ дней / known 7 дней) → форс-стейт или позже |
| 4 | Ещё 2 коллекции (идиомы/фраз.глаголы; другой язык), картинки Pexels, бейджи | ✅ идиомы/фраз.глаголы+частицы+бейджи+картинки+ru→de+TTS de; по ходу F14/F16 fixed, F15 |
| 5 | Экран коллекции: прогресс, повторный вход, «Слово дня» | ⬜ |
| 6 | Офлайн: авиарежим → триаж/тренировка → онлайн → /sync (курсор, тонстоуны) | ⬜ |
| 7 | Realtime-диалог: premium, полный диалог + ранний выход + «ещё раз» + лимит; ModelCost | ⬜ |
| 8 | Профиль: смена языка en↔ru, настройки, выход/вход | ⬜ |
| 9 | Негативные: без сети на старте, повторная регистрация, лимит генераций (3/день) | ⬜ |
| Д2 | SRS-петля (due, интервалы, цикл повторения, прогресс) | ⬜ (день 2) |
| Д2 | Удаление аккаунта — САМЫЙ ПОСЛЕДНИЙ шаг | ⬜ (день 2) |

---

## ДЕНЬ 2

### Вводные (ночные события вне батча, согласовано)
- **F1-регрессия:** батч-аккаунт пережил незапланированный релогин + повторный онбординг (онбординг при каждом входе; фикс `onboarded_at` в работе у другой сессии). Локальный drift очищался.
- **Проверка целостности по серверной БД ✅:** solonyna — 1 ряд, id/created_at без изменений (дубля нет); профиль native ru / **en / B2 / 30** / free, updated_at 15:06:59 (онбординг не переписал); reviews **41**, progress **30**, triages **33**, свои коллекции **3** — всё цело, лишних записей нет. (total users 5 — за ночь появился ещё один ЧУЖОЙ юзер, не solonyna; baseline-дрейф, отмечено.)
- Накатаны (другая сессия): `8ba83e0` — /sync-дельта несёт триаж-вердикты (проверить: после resync triagedTerms восстановились, вчерашние «не знаю» не переспрашиваются); `877a23f` — пустая learn-сессия при исчерпанной квоте → «дневной лимит достигнут» (адресует F13; в `session_screen.dart` появился флаг `learn` + `sessionDailyNewLimit`). Стор получил контент + sync подписок.
- Важно: в UTC ещё 7 авг (клиент показывает 8-е local) → квота генераций и introduced_today сброс в 00:00 UTC (03:00 local).

### Dev-инструмент времени (для day-2 без ожидания)
- `app/Console/Commands/BatchAgeProgressCommand.php` — `batch:age-progress {email} {--hours=N} {--reviews} {--force}`. APP_ENV=local only, подтверждение (или `--force`), сдвигает назад `due_at/updated_at/last_reviewed_at` (+ reviews при `--reviews`). Клиенту нужен ПОЛНЫЙ resync. Вне модулей (`App\Console\Commands`) → deptrac uncovered, 0 нарушений; stan чисто. Зарегистрирована в `bootstrap/app.php`.

### Forced-time лог (условия замера)
- **[FT-1] age-progress −20h** (progress only) → все **22 learning** стали due сейчас; review (8) остаются будущими. Замер SRS-петли ведём при этом условии.

### D2.1 — full resync + вводные (device)
- **F1-регрессия подтверждена на устройстве:** после релогина СНОВА показался онбординг. Причина — мой день-1 фикс чистит `onboarded` при выходе → пере-онбординг на каждом релогине. Правильный фикс (серверный `onboarded_at`) — у другой сессии, заменит клиентский флаг. Отмечено как согласованное отступление.
- **SRS due долетели через full resync ✅:** Home «Review 22»; Managing Work Calls «Review 9» (15·9 due, Familiar 12/In progress 3); Opening a Bank Account «Review 13» (18·13 due, Familiar 18).
- **8ba83e0 (триаж в /sync) ✅:** разобранные коллекции показывают **Review, не Triage** — вчерашние «не знаю» не переспрашиваются. Немецкая (не триажена) осталась бы Triage.
- WOD кросс-язычный (немецкое «Ich hätte gern die Speisekarte»).
- **Streak/активность = 0** после релогина (было 1) — F18 подтверждён на день-2 (сервер reviews целы, клиентский показ сброшен).
- Снапшот ДО SRS-петли (Opening a Bank Account): 13 learning/int1/reps1 (due) + 5 review/int3-5/reps2 (не due).

### D2.2 — SRS-петля (замер интервалов) ✅
Review 13 (банк), все верно → снапшот ПОСЛЕ:
- **9 learning → review**, interval 1→**3–5**, reps 1→2 (correct+**Good** → GRADUATING_INTERVAL 4, фазз 3–5). ✅
- **4 learning остались learning**, interval 1, reps 1→2 (correct+**Hard** → LEARNING_STEP 1 = «tomorrow»). ✅
- 5 не-due review (вчерашние) — не тронуты. ✅
- Итог: 13 Reviewed/0 New/0 Mistakes, **Streak → 2**. Sm2-переходы корректны, полный цикл повторения работает. **F19 виден:** «in 4/5 days» = now+N (точный таймстамп, не начало дня).
- **⚠️ наблюдение (F18-кластер):** в итоге сессии «Daily goal 0/30» ПРИ 13 повторениях в этой же сессии, хотя Streak обновился до 2. Локальный `DailyActivity` bump, похоже, не отражается после ночного wipe/онбординга (сервер reviews целы, streak с сервера обновился, но goal-счётчик из локального DailyActivity = 0). Проверить в задаче F18.

### D2 findings
- **F1-регрессия — НУЖЕН фикс здесь (другой сессии нет).** Онбординг при каждом релогине (мой день-1 фикс чистит `onboarded` при выходе).
  - **✅ FIXED IN RUN — серверный `onboarded_at` (согласован вариант 2).** Backend: миграция `2026_08_08_120000_add_onboarded_at_to_profiles` (+бэкфилл `onboarded_at=updated_at WHERE updated_at<>created_at` — батч-аккаунт покрыт: `onboarded_at=15:06:59`, 4/5 профилей, 1 pristine=null); `ProfileInput.onboarded` → `EloquentProfileUpdater` штампует один раз (не перезаписывает); `ProfileView.onboardedAt`→`ProfileResource`/`UserViewMapper`; `UpdateProfileRequest.onboarded`; OpenAPI обновлён; 3 новых теста (`ProfileOnboardingTest`), `composer test` **341 зелёный**, stan/arch чисто. Client: `Profile.onboardedAt` (fromJson+toJson для офлайн-кэша), гейт `onboardedProvider` из `user.profile.onboardedAt`, онбординг шлёт `onboarded:true`, keychain-флаг удалён из `token_store`; `flutter analyze` чисто, **188 тестов**. Гейт теперь привязан к аккаунту (переживает wipe/reinstall/устройства). **✅ DEVICE-VERIFIED (шаг D2.3):** logout+login → **сразу Home, онбординг НЕ показался**. F1 закрыт правильным (серверным) способом; день-1 клиентский фикс (clear-on-logout) снят.
- **Примечание по коммитам:** `openapi.yaml` в дереве СМЕШАН с чужой стор-работой (`/store/collections/{id}/preview`), поэтому мой `onboarded_at` в OpenAPI НЕ закоммичен со мной — уйдёт вместе со стор-коммитом.
- **F20 (perf):** после верного ответа и перехода к следующей карточке — небольшие подвисания в анимациях/переходах (сообщил пользователь). Возможные причины: цепочка setState(_answered)→AnimatedSize фидбека→async-загрузка фото→TTS→сброс скролла + AnimatedSwitcher слайд. Нужен профайл (release, устройство). Записано, не чиню без замера.

---

## ИТОГ ДНЯ 1 (пауза здесь)
Закрыты блоки **0–4**. Аккаунт `denys.solonyna@yalantis.net`: 3 коллекции (bank 18, work-calls 15, german 10), 33 термина в прогрессе, квота генераций 3/3, streak 1.

**Пофикшено вживую (8, все с зелёными гейтами — analyze/stan/arch/tests):**
- **F1** онбординг per-account (Keychain `clear()` чистит `onboarded`).
- **F8** после «не знаю» новые слова недостижимы → ветка CTA «Учить N» (due→learn→triage→practice).
- **F9** «Дальше» уезжала под фолд → закреплённая нижняя кнопка (v2; авто-скролл v1 не спас).
- **F10** mute-свитч глушил TTS → аудиокатегория `.playback`.
- **F11** урезанный итог сессии (нет списка/цели/Done) → `IntrinsicHeight` (release-only layout-blowup).
- **F12** двойной «Next» в word_bank → «Проверить»→«Дальше».
- **F14** генерация падала при одиночном уровне+базовой теме → CEFR-полоса = предпочтение, не гейт (`DraftValidator`).
- **F16** TTS читал язык профиля, не коллекции → `_speak` берёт `collection.targetLang`.

**В логе как findings (не чинили):** F3 (флаг English cosmetic), F4/F5/F6 (UX генерации: фейк-инпут, лендинг на Home, дубль-карточка — самолечится), F7 (картинки доезжают поздним синком), F13 (латент: «Учить N» пустая при исчерпанной квоте), F15 (залипшая клиентская квота после фейла).

**Заведены задачи (чипы):** все флаги (`task_b8a136b3`), Слово дня как фича (`task_b66a404f`), параллельные Pexels (`task_b72e350f`), значки языковой пары + смешанные языки (`task_6cc57279`).

**Не device-verify (перенос):** typing-тренажёр — review-state достаётся только через 5+ дней (или форс `state=review,due=now` в БД), в окно д1/д2 натурально не попадает.

**Осталось (след. заходы):** блок 5 (экран коллекции глубоко: прогресс/повторный вход/Слово дня), 6 (офлайн /sync), 7 (realtime-диалог premium), 8 (профиль en↔ru), 9 (негативные: без сети/повторная регистрация/лимит генераций — часть UI лимита уже видели), **день-2 SRS** (due завтра: 13+ learning), удаление аккаунта — самый последний.

---

## Блок 0 — преполётный чек ✅

- **backend2 контейнеры**: wt_app (:8001→8000), wt_db (:5433), wt_horizon, wt_redis, wt_ngrok — все `Up 24h`.
- **ngrok**: `https://greedily-thermos-finer.ngrok-free.dev → http://app:8000` (backend2). Статик-домен, стабилен.
- **mobile API_BASE_URL**: `config.dart` default = тот же ngrok-домен; клиент добавляет `/api/v1`. ✅ бьёт в backend2.
- **Миграции**: все Ran, последний batch 8 (`add_result_to_practice_dialogs`). Нет pending.
- **env**: `PRACTICE_DRIVER=openai` ✅, `APP_ENV=local`, `DB=pgsql wordtrainer`. (APP_DEBUG on — полный trace в ошибках, ок для dev.)
- **Роуты `/api/v1`**: auth/google, auth/me (GET+DELETE), collections CRUD+items, generations(+{id}), practice/*, profile(PUT), reviews/batch, stats, store/*, study/sessions+progress, sync(+cursor), terms/{id}/regenerate-example, triage/queue+batch. Все на месте.
- **Чистота аккаунта**: `SELECT … users WHERE email ILIKE '%solonyna%'` → **0 строк**. Следов нет.
- **Baseline БД** (существующие данные Дениса, не трогаем): users=3, collections=8, terms=301.

Открытый вопрос по env перед стартом: подтвердить, что `flutter run` запущен на телефоне
(`cd mobile && flutter run --release -d 00008110-000A7CCC3492801E`) и action-лог виден.

---

## Прогресс блока 1
- **1.1** вход Google → создан `users 01KZEAYF…ZMXC` + `profiles` (дефолты). Онбординг проскочил → F1.
- **1.2** фикс F1 + пересборка + Sign out + повторный вход → онбординг показался. Дубля юзера нет.
- **1.3** онбординг пройден с не-дефолтными значениями (English / **B2** / **30**) → `PUT /profile` записал `target=en, cefr=B2, daily_goal=30`, `updated_at` 15:06:59. Главная = `0 / 30`. ✅ device-verified.
- **Слово дня**: у пустого аккаунта отсутствует — **by design** (`word_of_day.dart` берёт из локальных терминов, их нет). Не баг. Пере-проверим в блоке 5, когда появятся коллекции.

## Будущие задачи (заведены чипами по просьбе пользователя)
- **Все флаги**: painted-флаги для всех target-языков (сейчас только en/de/es/fr/pt), + фикс F3 (English-флаг). `task_b8a136b3`.
- **Слово дня как фича**: отдельный API + курируемая таблица интересных слов по уровням CEFR, исключая слова из коллекций юзера. `task_b66a404f`.
- **Параллельные картинки**: `AttachCollectionImagesHandler` тянет Pexels последовательно (~19 вызовов на 18 слов); распараллелить Guzzle Pool. `task_b72e350f`.

## Прогресс блока 2
- **2.1** Генерация «Открыть счёт в банке», large=22, levels ["B2"], en → request `01KZED7TTFJFBYGX3V2KTR4XSD` **succeeded** за ~52 c. **delivered 18/22** (недобор). Коллекция `01KZED9D35WRVW5ZAJNZQZBJA7` «Opening a Bank Account», 18 items. Бейдж «18 of 22» на карточке — ✅ верно.
- Server-side: обложка (Pexels URL) + **18/18** слов с картинками прикреплены.
- **2.2** pull-to-refresh → **F6 и F7 самолечатся**: дубль-карточка ушла, обложка + все фото проступили. Экран коллекции: обложка, «18 words», сегменты Confirmed 0/Familiar 0/In progress 18, CTA «Triage 18 · New words in this set», список из 18 слов с миниатюрами/бейджами/переводом/IPA — всё рендерится. ✅
- **Content-note (не баг устройства):** двухсловные коллокации (monthly fees, bank teller, customer service, joint account) помечены бейджем WORD, а proof of address / credit history — PHRASE. Классификация «слово/фраза» у LLM непоследовательна. Низкий приоритет.

## Блок 2 — модель «сколько слов в сессии» (по коду, до device-проверки)
Логика отбора сессии (backend2 Learning):
- **Триаж «не знаю» (unknown)** пишет ТОЛЬКО строку в `term_triages` (append-only), `user_term_progress` НЕ создаёт — термин остаётся «новым» (`TriageTermsHandler.php:133-144`). «знаю»→`known`+due-верификация; «не уверен»→`learning` due-now.
- **Сборка сессии** (`BuildStudySessionHandler.php:56-58` + `GetDueTermsHandler.php:52-60`): `size` зажат [1,100]; `newRemaining = min(size, daily_goal − introduced_today)`; отбор = **due раньше new**, new заполняют остаток слотов и режутся квотой новых-в-день.
- **`newTermsPerDay` = profiles.daily_goal** (клэмп [0,100], дефолт 20) — у нас **30** (`IdentityLearnerProfileReader.php:32-40`).
- **Клиент всегда просит `limit=20`** (дефолт `SessionScreen.limit`, оба вызова его не передают — `session_screen.dart:33`).

→ Ожидание для «22 слова, все „не знаю“»: первая тренировка = `min(20, 30, 22)` = **20 карточек**; 2 слова уходят в следующую сессию. Это **кап размера сессии (20)**, не дневная квота (30). Проверяем цифры на устройстве → вердикт bug/задумано.

## Блок 3 — модель режимов и достижимость сегодня
- Сессия «Learn 18» в БД: `study_sessions` is_practice=**false**, composition=**18**, scoped → ✅ фикс F8 подтверждён и на уровне данных (старая practice-сессия была composition=0).
- Шкала режимов (`ExerciseSelector`): new→**multiple_choice**, learning(многословный)→**word_bank** (одно слово→MC), review→**typing/listening/cloze**, known→**typing**.
- Планировщик (`Sm2Scheduler`): new+Again→learning **0д (due сейчас)**; new+Good→learning **1д (завтра)**; learning+Good→review **4д**.
- **Достижимо сегодня:** MC (весь заход) + word_bank (2-й заход после Again на многословных). **typing** — только review-state (4+ дня) или known-верификация; натурально не сегодня → в день-2+ или трюк через «знаю». Фраз. глаголы с частицами — в блоке 4 (тут их нет).

### Прогресс блока 3 (device)
- **3.1 multiple_choice** ✅ — сессия «Learn 18» (comp 18, is_practice=false). Верно→шалфейное подчёркивание+галка+«See it again tomorrow» (grade good/hard, interval 1); неверно→терракота+×+верный подсвечен+«пишется сама»+«See it again today» (grade again, interval 0). reviews синкаются инкрементально с client_seq/exercise_mode. Итог сессии1: 9 good/3 hard/6 again → progress 6×interval0(сегодня)+12×interval1(завтра).
- **3.2 word_bank** ✅ — 2-й заход «Assemble» (comp 6, is_practice=false, из 6 due-сегодня). Многословные → чипы, сборка в строку, выцветшие копии, «tap to send back». Next без скролла. Частиц-дистракторов нет (нет фраз. глаголов — к блоку 4).
- **3.3 typing** ⏭ перенос в блок 4 (review-state недостижим сегодня; ловим через вердикт «знаю» + `regenerate-example`/«Не помню»).
- **3.4 фидбек/автопроизношение** ✅ (кроме F10 — mute-свитч глушит).
- **3.5 итог** ⚠️ F11 (урезан, нет Done).

## Блок 4 — прогресс
- **4.1** Генерация «Managing Work Calls with Colleagues» (req `01KZEHX8…`, size 15, en) → **15/15** (полная доставка). Коллекция `01KZEHXPNERW1H6DRS4SRJWEAA`. Типы: phrase 10, **phrasal_verb 2 (call off, set up a call)**, word 2, **idiom 1 (touch base)**.
- **4.2** pull-to-refresh → обложка+фото проступили; бейджи device-verified: **PHRASAL VERB** (call off), **IDIOM** (touch base), WORD (postpone, conference call), PHRASE (остальное). ✅
- **typing (3.3):** верификация «знаю» due через 7/90 дней, review-state — 5+ дней → typing НЕ достижим в окно день-1/день-2 натурально. План: форс-обновить одному слову `state=review, due=now` в БД под конец блока 4, чтобы device-verify UI typing + «Не помню».

## Решения по ходу (не findings)
- **Фото в фидбеке при ВЕРНОМ ответе — НЕ добавляем** (согласовано). Верный ответ = быстрый такт, подкрепление уже есть (автопроизношение), фото ценнее при ошибке (где память дала сбой); разрастание замедлит сессию 30+ шагов. Фото при ошибке остаётся во всех режимах. Идея на потом — ок.

## Findings

### F19 — SRS-интервал считается точным таймстампом, а не календарным днём · **design (пользователь; замер ведём по текущей логике)**
День 2. Ответ в 19:00 с интервалом 1 → `due_at = 19:00 следующего дня`, а не «начало следующего дня». Т.е. слово недоступно для повторения утром/днём, хотя по SRS «1 день» = следующий календарный день (обычно due с 00:00 локального дня). `Sm2Scheduler::next` (`app/Modules/Learning/Domain/Service/Sm2Scheduler.php:143`): `$dueAt = $now->add(new DateInterval('P{interval}D'))` — прибавляет к точному моменту. Последствие: карточки «уплывают» на время суток ответа; вечерний ученик получает due вечером, а не с утра.
- **Fix-направление (после SRS-замера):** округлять due_at к началу дня (локального/юзерского TZ) для интервалов ≥1 дня (0-day/learning-шаги оставить как есть — они внутридневные). Обсудить TZ (у юзера нет TZ-поля — брать из клиента или UTC-день).
- Нумерация: пользователь назвал это «F18»; в этом логе F18 уже занят (сброс активности при релогине) → записано как **F19**.

### F18 — статистика активности (Reviews today / This week / график) сбрасывается после релогина · **bug / пробел**
День 2, после релогина. Progress-экран: LEARNED TOTAL 0, THIS WEEK 0, REVIEWS TODAY 0, пустой месячный график — при том что сервер сегодня = **41 review (30 слов)**, стрик 1, плотность «All 43 · Familiar 30 · In progress 13» (корректно).
- **LEARNED TOTAL 0 — корректно** (mastered=0, ни у кого interval≥21; «усвоено» = зрелость SRS).
- **Reviews today / This week / график — читают клиентский `DailyActivity` (drift), который `clearAll` стирает при выходе; серверной таблицы дневной активности НЕТ → бэкфилить нечем.** Стрик и плотность выживают (пересчёт из синканутого прогресса), активность — нет. Асимметрия видна пользователю как «статистика пропала».
- Handoff это отмечал в known-limitations («DailyActivity accumulates from now, no backfill» / «stale offline reviews-today»), но UX выглядит как баг.
- **Fix-направление:** сервер отдаёт per-day активность (эндпоинт/поле в `/sync` или `/study/progress`), клиент бэкфилит `DailyActivity` при синке; либо Reviews-today тянуть из серверного `reviews_today`, а график — из серверной истории. Не влияет на SRS-петлю (день-2).

### F17 — «Free practice» не даёт тренировать выученные-не-due слова (пустая, пока ничего не due) · **bug / пробел**
После дня 1. Банк-коллекция: 18 в SRS (13 learning/5 review), **due_now=0**. Тап «Free practice» → пустая сессия «Nothing to review here yet». Practice (`isPractice=true`, `BuildStudySessionHandler:56` newRemaining=0) отдаёт только due-термины (`GetDueTerms.dueAmong`), а не «дай позаниматься уже выученным». Смысл свободной тренировки — дрилить слова В ЛЮБОЙ момент без влияния на прогресс; сейчас она доступна только когда что-то и так due (тогда есть и «Повторить»). Пользователь верно заметил: практика прогресс не двигает — это ок, но самой практики по требованию нет.
- **Fix-направление (backend):** для `isPractice` собирать пул из STUDIED-терминов скоупа (есть progress-ряд) **игнорируя due_at**, cap по size, без нью-слов и без планирования. Новый reader `studiedAmong(user, candidates, size)` + ветка в `BuildStudySessionHandler` + тесты/arch. (Ранее помечено как «открытый хвост» при F8.)

### F16 — TTS читает язык профиля, а не язык коллекции · **bug → FIXED IN RUN**
Шаг 4.5e. Спикер на немецкой коллекции читал немецкий текст НЕ по-немецки. Причина: `collection_detail_screen._speak` и `session_screen._speak` брали `profile.targetLanguage` (=en), а не язык **коллекции** (de). Язык живёт на коллекции (`WordCollection.targetLang`), но в озвучку не прокидывался.
- **FIXED (клиент) ✅ DEVICE-VERIFIED (шаг 4.5f):** `_speak` на экране коллекции → `_collection?.targetLang`; `SessionScreen` получил параметр `targetLang`, `_openSession` прокидывает `_collection?.targetLang`, сессия озвучивает по нему (кросс-коллекционная — фолбэк на профиль). После пересборки спикер немецкой коллекции звучит по-немецки. `flutter analyze` чисто, 163 теста.
- **Оговорка (в задачу `task_6cc57279`):** кросс-коллекционная сессия языка не знает (`SessionCard` не несёт `target_language`) → часть карточек озвучится языком профиля. Правильный фикс — per-card `target_language` на бэкенде ИЛИ скоуп тренировки по одному языку.
- **Плюс по просьбе пользователя (`task_6cc57279`):** значки языковой пары ru→de/ru→en на карточке коллекции + решение по смешанным языкам в одной тренировке.

### F15 — клиентская квота генераций «залипает» после упавшей генерации · **bug (клиент)**
Шаг 4.5c. После 2 succeeded + 2 failed немецких экран создания показал **«Out of generations for today»**, Generate серый. Но сервер (`EloquentGenerationQuota::usedOn`, проверено tinker) = **used=2, remaining=1** (failed корректно исключены). Клиент закэшировал квоту в момент, когда german#2 был **pending** (pending считается: `status<>'failed'` → used=3), и **не обновился** после его падения. Слот реально свободен, но кнопка серая.
- **Fix-направление:** инвалидировать `generationQuotaProvider` при переходе генерации в терминальный **failed** (сейчас, видимо, только при succeeded), и/или рефетчить `/me` при открытии create-экрана.
- **Бонус:** это же — UI исчерпания квоты для блока 9 (серый Generate, «resets at 03:00», Premium-строка «up to 20 a day», «Build a collection manually») — визуально подтверждён здесь.

### F14 — генерация падает при одиночном уровне + базовой теме (точный CEFR-фильтр) · **bug / design**
Шаг 4.5. Генерация «Заказ еды в ресторане», target=de, levels=["B2"] → **failed: «only 7 usable items after validation»** (`DraftValidator.php:75`, MIN_ITEMS=8). Модель СГЕНЕРИРОВАЛА немецкие items (title/слова/IPA/ru-перевод в raw), но `DraftValidator` с `levels=["B2"]` строит диапазон **[B2,B2]** (`levelRange`, точное совпадение) и отбрасывает всё вне B2 (`:50-54`). Ресторанная лексика — A1–A2 → под B2 прошло 7 < 8 → весь драфт отклонён.
- **Не про немецкий как таковой** — тот же эффект для любой базовой темы при одиночном высоком уровне. Онбординг/профиль дают ОДИН уровень, create берёт его → частый кейс.
- Клиент отработал ок: карточка «Didn't work out · No generation was spent · Retry/Hide», квота НЕ потрачена.
- **Fix-направления:** (1) трактовать одиночный уровень как полосу (±1 или «≤ уровень»), а не точное совпадение; (2) при отклонении по уровню — авто-ретрай с расширенным диапазоном либо подсказка юзеру «добавьте уровни / тема проще выбранного уровня»; (3) не дефолтить create ровно на один уровень профиля. Retry как есть (тот же B2) упадёт снова.
- Обход для прогона: пере-создать с диапазоном A2–B2.
- **Подтверждено 2× детерминированно:** повтор с B2-only → снова failed («only 3 usable» / ранее «only 7»). Варьируется лишь число (рандом модели), исход — всегда отклонение. Оба фейла квоту не потратили (2/3 использовано).
- **✅ FIXED IN RUN + DEVICE-VERIFIED (пользователь: «выглядит как баг, не должно такого быть»).** `DraftValidator`: CEFR-полоса теперь **предпочтение, а не жёсткий гейт** — собираем `$inLevel` и `$all`; если in-level < MIN_ITEMS, берём `$all`, а не отклоняем драфт. Жёсткий отказ — только для реально короткого/битого ответа. Хелпер `withinLevel`. Тесты 10/10, `stan` 0 / `arch` 0, **Horizon перезапущен**. **Device-verify (шаг 4.5d):** та же немецкая генерация **levels=["B2"]** → **succeeded 10/10** «Essen im Restaurant bestellen» (немецкие фразы + ru-перевод + IPA + обложка). Ровно падавший сценарий теперь проходит.

### F13 — «Learn N» может дать пустую сессию при исчерпанной дневной квоте новых · **design-question (латентный, не триггерился)**
Наблюдение (блок 4). `newTermsPerDay=daily_goal=30`; за день ввели 30/30. Клиентский `learnable` считает новые-триаженные термины без учёта серверной квоты. Если окажется `due=0 && learnable>0 && квота исчерпана`, CTA покажет «Learn N», но серверная сессия введёт 0 новых → пустая (как F8-эхо). Сейчас не сработало (due=5 в приоритете). Фикс-направление: гейтить «Learn» остатком дневной квоты (из `/me`), либо на пустой сессии показывать «дневной лимит достигнут», а не пустой экран. Записано, не чиню сейчас.

### Пачка фиксов F9–F12 (согласовано «пачкой сейчас») — наложены, analyze чисто, 163 теста зелёные
- **F10 FIXED** — `Pronouncer._configureIosAudioSession()` ставит iOS-аудиокатегорию `.playback` (duckOthers), один раз, iOS-only через `defaultTargetPlatform` (без dart:io — web/preview не ломается). Теперь произношение звучит и при mute-свитче.
- **F11 FIXED** — Row счётчиков итога обёрнут в `IntrinsicHeight` (`session_screen.dart`). Корень: `CrossAxisAlignment.stretch` под безграничной высотой скролла → в release (ассерты off) Row распухал и выталкивал goal-card/список/Done за экран. Теперь итог рендерит всё, включая **кнопку Done**.
- **F12 FIXED** — предсабмитная кнопка word_bank переименована `sessionNext`→`sessionCheck` («Проверить»/«Check»); «Дальше» остаётся только в фидбеке. Два шага — две подписи.
- **F9 FIXED (v2) ✅ DEVICE-VERIFIED (шаг 4.4b)** — авто-скролл (v1) не спас (фидбек-фото грузится async). Переделано на **закреплённую нижнюю кнопку «Дальше»** (`_NextBar` вне скролла, флаг `_answered`; кнопка удалена из фидбек-блока, `onNext` убран из карточки). На устройстве: «Дальше» прибита к низу, видна без скролла.
- **F12 ✅ DEVICE-VERIFIED (шаг 4.4b)** — word_bank: предкнопка «Проверить», после проверки — закреплённая «Дальше».
- **Частицы-дистракторы ✅ DEVICE-VERIFIED (4.4b)** — карточка **call off** (phrasal verb): чипы **off / call / in** — «in» лишняя частица-приманка (ChipShuffler для phrasal_verb).
- **✅ DEVICE-VERIFIED (шаг 4.4a): F10** — озвучка звучит в mute-режиме. **F11** — итог полный: Daily goal 24/30, Streak 1 day, список 15 (стоп — 12) слов с ✓/× и сроками, «Struggling → New example», кнопка **Done**. **F9 v1 НЕ сработал** → переделан на закреплённую кнопку (v2, выше), нужен новый device-verify.
- **Наблюдение (дневной кап вживую):** сессия была «1 of 12», не 15 — `newTermsPerDay=30`, за день уже введено 18 → осталось 12. 3 слова коллекции остались новыми. `introduced today = 30/30`. Ровно модель блока 2.

### F12 — word_bank/typing: «Next» нажимается дважды (проверить, потом дальше) под одной подписью · **design-question (UX) → FIXED**
Шаг 3.2. В word_bank (и, вероятно, typing) после сборки фразы первый тап по «Next» отправляет/проверяет ответ (показывает фидбек), второй тап по тому же «Next» переходит к следующей карточке. Обе стадии — одна кнопка с подписью «Next», из-за чего кажется, что первый тап «не сработал». В multiple_choice сабмит происходит по тапу варианта (один Next = дальше). Предложение: первая стадия — «Проверить/Check» (или авто-сабмит при полностью собранной фразе), вторая — «Дальше/Next». Не блокер.

### F11 — экран «Session complete» урезан: нет списка слов, дневной цели, «Новый пример», кнопки Done · **bug**
Шаг 3.2-подготовка. После 18 карточек итог показывает ТОЛЬКО три счётчика (18 Reviewed · 18 New · 6 Mistakes). Ниже пусто. По спеке A3.8 (12e) должны быть: блок **дневной цели** (сегодня/30 + стрик), **список слов** с иконками результата и относительным сроком, блок **«Проседает: {term} → Новый пример»** (B6 `regenerate-example`), кнопка **«Готово/Done»**. Их нет. Выйти можно только **свайпом назад** (кнопки Done нет). БД при этом корректна (reviews 9 good/3 hard/6 again; progress 6×interval0 + 12×interval1; daily_activity должен быть 18). То есть данные есть, а UI итога их не рендерит. Не блокер прогона, но заметный регресс/недострой. Кандидат на фикс.

### F9 — кнопка «Next» уезжает под фолд после ответа · **design-question (UX)**
Шаг 3.1. После ответа панель фидбека (фото + IPA + пример) выталкивает «Next» вниз — приходится скроллить, чтобы дотянуться до кнопки. Ожидается: «Next» доступна без скролла (закрепить внизу / авто-скролл к ней после ответа). Не блокер.

### F10 — автопроизношение молчит при включённом тумблере «без звука» · **bug (UX)**
Шаг 3.1. При обычном режиме произношение звучит; когда физический переключатель iPhone стоит на «беззвучно» (mute), автопроизношение (и спикер) **не воспроизводятся**. Для обучающего приложения произношение — это media-плейбек, а не звук уведомления: iOS-аудиосессия должна быть категории **`.playback`** (переопределять mute-свитч), как в плеерах. Сейчас, видимо, дефолтная `ambient/soloAmbient` → уважает mute. Много кто держит телефон на silent → фича фактически не работает. Кандидат на фикс (`flutter_tts` `setIosAudioCategory(.playback)` или нативная настройка AVAudioSession).

### F8 — после триажа «не знаю» новые слова недостижимы для изучения · **BUG (блокер обучения) — device-confirmed → FIXED IN RUN → device-verified**
Шаги 2.3–2.4. Отриажил все 18 слов как «не знаю» → на коллекции единственный CTA = **«Free practice»** → тап = **«Nothing to review here yet»** (пустая сессия). Сегмент при этом «In progress 18». Выучить 18 новых слов НЕЧЕМ.
- **Корень (цепочка):**
  1. Триаж `unknown` не пишет `user_term_progress` (`TriageTermsHandler.php:141-143` — «leave it new; full intro via multiple_choice»), слово помечается локально в `triagedTerms`.
  2. Клиентский CTA `due→triage→practice→none` (`home_cta.dart:35`, `collection_cta.dart:12`): после триажа untriaged=0, due=0 (нет progress-рядов), total=18 → падает в **practice**.
  3. Practice = `isPractice=true` → `newRemaining=0` (`BuildStudySessionHandler.php:56`), due=0 → сессия пустая.
- **Суть:** сервер готов отдать новые слова (`GetDueTermsHandler` берёт кандидатов без progress-ряда), но клиент **никогда не стартует non-practice сессию при due=0**. «Полный интро через multiple_choice» из комментария нечем запустить. `due` на клиенте = только progress-ряды past-due (`collectionsProgressProvider`), новые термины туда не входят.
- **Проверка БД:** `user_term_progress`=0, `reviews`=0, `term_triages`=18 unknown (client_seq 303→320). Всё сходится с моделью.
- **БЛОКЕР ПРОГОНА:** блоки 3 (тренажёры), 5 (прогресс после сессии), 6 (офлайн-тренировка), день-2 SRS — все требуют рабочей сессии с карточками. Без фикса дальше по обучающей петле не пройти.
- **Направление фикса (не «свободная тренировка сломана», а отсутствует ветка «Учить новые»):** в `computeHomeCta`/`computeCollectionCta` при `due=0 && untriaged=0 && newEligible>0` предлагать **non-practice** сессию «Учить N» (isPractice=false, вводит новые под дневную квоту), а practice оставить только когда всё изучено и ничего не due. Нужен счётчик newEligible на входе CTA + строки ARB + правка чистых тестов.
- **✅ FIXED IN RUN (согласован вариант 1 + приоритет пользователя).** Новый приоритет CTA обоих экранов: **due → «Учить N» → триаж → practice → none** (practice теперь достижим только когда всё в SRS → пустая кнопка на свежем наборе не показывается). Изменения:
  - DB: `watchLearnableByCollection()` (`app_database.dart`) — термины без progress-ряда, но с локальной строкой триажа = triaged-«не знаю», готовы к вводу. Провайдер `learnableByCollectionProvider`.
  - CTA: `HomeCtaKind.learn`; `computeHomeCta`/`computeCollectionCta` переставлены под новый приоритет + принимают learnable. «Учить N» открывает **non-practice** сессию (вводит новые под квоту 30).
  - UI: кейс `learn` в `_CtaButton` (главная + коллекция) и в hint на вкладке Collections; ARB ru+en (`homeLearnButton/Subtitle`, `collectionLearnButton/Subtitle`).
  - Тесты: home_cta (9) + collection_cta (F8-кейсы) переписаны; `flutter analyze` чисто, **`flutter test` 163 passed**. Схема drift не менялась (запрос по существующим таблицам). Ждёт device-verify после пересборки.
  - **Открытый хвост (отдельно, не в этом фиксе):** free-practice сессия сервер-side отдаёт только due-термины (`newRemaining=0`), т.е. может быть пустой даже когда слова в SRS, но ничего не due. Сейчас practice просто не показывается на all-new; но «просто позаниматься выученным вне графика» пока не поддержано — кандидат в отдельную задачу (practice должен брать изученные-не-due).
  - **✅ DEVICE-VERIFIED (шаг 2.5):** после пересборки Home = «Learn 18 words · New words — first study», коллекция = залитая «Learn 18 · New words — learn them». Пустой «Free practice» ушёл. Бонус: «Слово дня» появилось (online banking) — термины появились локально, by-design-пусто из блока 1 закрылось само. **F8: bug → fixed in run → device-verified.**

### F1 — онбординг пропущен для нового аккаунта · **bug → FIXED + verified**
Шаг 1.1. После входа `denys.solonyna@yalantis.net` приложение ушло **сразу на главную**, минуя 3-шаговый онбординг.
- Причина: флаг `onboarded` хранится в **Keychain** (`flutter_secure_storage`, `TokenStore._onboardedKey`), не в БД и не в drift. `TokenStore.clear()` (`mobile/lib/data/token_store.dart:25`) при logout/401 удаляет только `api_token` + `api_user`, ключ `onboarded` **не чистит**. Гейт `mobile/lib/main.dart:72` читает флаг.
- Следствие: как только ЛЮБОЙ аккаунт прошёл онбординг на устройстве, `onboarded=1` навсегда оседает в Keychain (переживает выход, повторный вход другим аккаунтом и переустановку iOS). Каждый следующий аккаунт онбординг не видит.
- Проверка БД: у `profiles` нет колонки onboarded; ряд создан на серверных дефолтах (native=ru, target=en, cefr=B1, daily_goal=20, tier=free). `users` +1 (`01KZEAYF22TK4BQV6FEQ61ZMXC`).
- Влияние на прогон: онбординг (A3.7 10b–10d) не получится device-verify без фикса `clear()` или ручной очистки Keychain.
- **РЕШЕНИЕ (согласовано):** фикс наложен — `clear()` теперь удаляет и `_onboardedKey` (`token_store.dart:25`, комментарий «device-batch F1»). Онбординг = per-account, забывается при выходе.
- **✅ VERIFIED (шаг 1.2):** пересборка → Sign out → повторный вход Google → онбординг **показался** (шаг 1/3 «Which language are you learning?»). Дубля юзера нет: тот же `id 01KZEAYF…ZMXC`, `created_at` без изменений, total_users 4 = baseline 3 + solonyna. Профиль на дефолтах (en/B1/20, native ru), онбординг ещё не финишировал. **F1 закрыт фиксом + device-verified.**

### F4 — инпут генерации на Home — «фейковое» поле · **design-question (UX)**
Шаг 2.1. Поле «e.g. a visit to the doctor» на Home выглядит как ввод, но тап просто перебрасывает на экран создания; напечатать/надиктовать прямо тут нельзя, введённый текст не переносится. Пользователь: неочевидно; хочет ввод/голос прямо на Home с переносом в create. (Handoff 0b обещает «chip text carried + field focused» — проверить, но сырой ввод текста на Home сейчас невозможен.)

### F5 — после «Сгенерировать» кидает на Home, а не на Collections · **design-question (UX)**
Шаг 2.1. Пендинг-карточка «Собираем…» живёт на вкладке Collections, а после сабмита приложение уходит на Home → нет видимого фидбека, что генерация пошла. Предложение: уводить на Collections (или показывать пендинг на Home).

### F6 — коллекция показана дважды на Collections · **design-question (UX)**
Шаг 2.1 (после синка). Одновременно видны ready-карточка генерации «Ready · fewer than asked · 18 of 22» И реальная синканутая коллекция «18 words · Triage 18». По коду это by design: в live-поллинге (`generation_controller.dart:197-206`) ready-карточка НЕ дропается при успехе (дроп только на следующем launch/resume в `reconcile()` `:115-118`), а `_sync()` уже подтянул коллекцию. Функционально ок и само схлопнется, но выглядит как дубль. Вопрос дизайна: не показывать обе сразу.

### F7 — обложка и фото слов не рендерятся до повторного синка · **bug (UX)**
Шаг 2.1. На плитке коллекции — плейсхолдер-звёздочка, хотя server-side обложка и 18/18 фото прикреплены. Причина: пост-успешный `_sync()` (`generation_controller.dart:205`) стреляет в момент `succeeded`, а `AttachImagesJob` дорисовывает картинки ПОЗЖЕ (последовательные Pexels-вызовы) → первый синк их не застаёт, авто-ре-синка нет → фото «доедут» только на pull-to-refresh/resume. Связано с `task_b72e350f` (параллель ускорит attach) + идея авто-ре-sync через N сек после succeeded. Проверяем pull-to-refresh на устройстве.

### F3 — флаг English в онбординге читается как нордический крест · **cosmetic**
Шаг 1.2, экран выбора языка изучения. Painted-флаг у «English» (круглый Union Jack-кроп) выглядит как синее поле с бело-красным крестом — на глаз ближе к исландскому/скандинавскому, чем к Union Jack. Остальные painted (Español/Deutsch/Français/Português) читаются верно; непокрытые языки — нейтральный серый кружок с кодом (UK/RO/IT/PL/TR). Низкий приоритет, прогон не блокирует.

### F2 — UI на английском, не на русском · **design-correct (закрыт)**
Шаг 1.1. Главная целиком по-английски («Daily goal», «Describe a topic…», Home/Collections/Progress/Profile).
- Причина: локаль = device (English) → `en` есть в `kSupportedLocales` → резолвится в английский. `profile.native_language=ru` на выбор UI-локали не влияет — так и задумано.
- **РЕШЕНИЕ (согласовано):** резолюция локали корректна, F2 = design-correct, не баг. День 1 идём на **en**. Проверку ru перенесли в **блок 8**: override=ru в Профиле + контрольный круг по главным экранам.

# Session handoff — snapshot

> **Overwrite this file each session. Snapshot of current state, not a growing log.**
> Read with `CLAUDE.md`, `ARCHITECTURE.md`, `.claude/skills/`, `deptrac.yaml`, `docs/ROADMAP.md`.
> **`docs/DECISIONS.md` (в корне репозитория) читается ПЕРВЫМ шагом любого наряда** — это реестр
> действующих решений; если постановка ему противоречит, стоп и доклад архитектору.
> `triage-contract-findings.md` заморожен. Детали живых прогонов: `docs/device-batch-run1.md`,
> `docs/qa/PLAYBOOK.md`.

Branch: `main`. Last updated: 2026-08-24.

> **LATEST TRACK — фаза A мультиязычности, наряды A-2 → A-4. Код фазы A закрыт.**
> Язык карточки диктует ПАРА КОЛЛЕКЦИИ, а не профиль (A-2). Поиск умеет произвольные пары и
> называет роли явно (RS-3, A-3, A-3.1, A-4). Справочные языки (zh/ja) отрезаны от пула на сервере
> и от кнопок на клиенте (A-4). Бейдж пары стоит везде, где коллекция видна (A-4).
>
> **Живой контент не изменён ни одной строкой.** Мораторий на витрину в силе (пп. 68–69).
>
> **Что НЕ закрыто фазой A:** п. 138 (`term_examples.lang` + отдельная таблица переводов примеров)
> — принят, но код за ним не пошёл ни в одном наряде. Это первый кандидат в следующий наряд.

---

## Что готово этим треком (с хешами, ветка `main`)

**A-2 (24.08) — язык карточки от пары коллекции.**
- `688d23e` доки: пара — свойство КАЖДОЙ коллекции, гейт пары на записи, профиль только дефолт.
- `629ce83` **`CardLanguageResolver`** — одна точка «на каком языке эта пачка карточек»:
  скоуп-коллекция → её пара, пул → пара коллекции каждого термина, профиль → только для термина,
  у которого коллекции не осталось. `TermContentReader::byIds` принимает `SupportLanguages` —
  язык НА ТЕРМИН, а не один скаляр на пачку. Закрывает п. 137 и спорный п. 1 ARC-1.
- `132e2cc` доменный гейт пары на записи «термин → коллекция» (`Collection::addTerm`,
  422 `term_language_mismatch`) — п. 141.
- `377cbfb` **`LanguageModeSupport`** — справочник «язык → доступные режимы» В КОДЕ, эффективная
  доступность = пересечение с админской матрицей (п. 130). Матрица языком не расширяется.
- `ceb1051` пять мёртвых дефолтов конфига станка.

**A-3 / RS-3 / A-3.1 (24.08) — поиск в произвольной паре.**
- `16ed0ca` строки языковой пары, «папка» → «коллекция» (п. 119).
- `8a4f83b` две пилюли пары со свапом над полем поиска; `2f40a8e` добавление по паре
  (фильтр коллекций, создание в паре, 422 в лицо).
- `12ec2c3` реестр пп. 144–147; `afc3a93` **`LanguageRoles`** — роли считаются из справочника и
  капабилити, `config/languages.php` + `APP_TARGET_LANG` + `APP_NATIVE_LANGS` удалены (п. 145).
- `d993f2f` хит поиска отдаёт перевод СТРОГО на языке поддержки запроса, без фолбэка (п. 146).
- `ca5f06d` пилюли читают `targets`/`natives`; `29c11f6` живое тело `/search/languages` запиннено.

**A-4 (24.08) — справочные языки, роли от клиента, бейдж пары.**
- `b06ef1f` **Ч.1, backend.** `LanguageRoles::isReference()` — справочность ВЫВОДИТСЯ из
  капабилити (язык без единого тренажёра), а не хранится. Пул отказывает обеими дверьми:
  `PUT /pool/terms/{id}` → 422 `reference_language_term` (гейт в `EnrollTermHandler`, через него
  ходят и «Учить это слово», и сохранение из поиска); `/triage/batch` кладёт такой свайп в новый
  счётчик `rejected` и не пишет ни лога, ни прогресса, пачка при этом не падает целиком (п. 101).
  `/sync` несёт вычисляемый `is_reference` у коллекции. `/study/progress` получил разрез
  `by_language` РЯДОМ с `data` (п. 139), справочные термины не считаются нигде. Поиск принимает
  необязательный `taught_side` (`source|target`) — роли называет клиент, тай-брейк по профилю
  (п. 147) остался фолбэком; сторона, названная необучаемым языком, — 422 `taught_side_not_taught`.
- `1269770` **Ч.2–Ч.3, mobile.** Бейдж пары на полке «Мои», карточке стора, в шапке экрана
  коллекции и над карточкой в тренировке (в сессии одной пары — один раз в шапке, в смешанной —
  на карточке, п. 128). Пара берётся из ЛОКАЛЬНОГО зеркала (`AppDatabase.pairByTerms`, первая
  коллекция термина по id — как на сервере), поэтому бейдж есть и офлайн. Справочные коллекции:
  `is_reference` зеркалится колонкой (schemaVersion 17 + пересинк), тренировки/триажа/практики/
  зачисления нет, вместо прогресса — счёт слов и объяснение. `word_bank` получил «Не помню» тем же
  каналом, что `scramble`. `MiniFlag`: дорисованы `pl`, `it`, `ru`. `PLAYBOOK` прил. В: панель
  симулятора может быть выключена флагом раскатки ЦЕЛИКОМ.
- `1d91d7a` **бейдж пары — флаги в тихой плашке.** Решение владельца с экрана; реестр п. 148,
  `tokens.html` §4б и правило 14 уточнены. Харнесс `mobile/tool/pair_badge_preview.dart` остаётся.

## Device-verified vs code-only

| Пункт | Как | Статус |
|---|---|---|
| A-2: `folder` из пары en←es приходит с переводом `carpeta` | `POST /study/sessions` под QA-токеном: `prompt: carpeta`, `answer: folder` при `profiles.native_language = ru` | ✅ по API |
| A-4 Ч.1 целиком (пул, триаж, `/sync`, `/study/progress`, `taught_side`) | `ReferenceLanguageTest` (6) + `SearchTaughtSideTest` (6) + `composer test` 1495 | ✅ тестами |
| Бейдж пары на полке «Мои» | **виден на экране** (кадр от 19:34, редакция «голые флаги») | ⚠️ снят на ОТМЕНЁННОЙ редакции |
| Бейдж пары: принятая редакция (флаги 12 в плашке) | widget-тест пинит плашку и порядок флагов | ⚠️ **code-only, на экране не снят** |
| Бейдж пары в шапке коллекции | **виден на экране** (кадр `Praca po polsku`, редакция «голые флаги») | ⚠️ снят на ОТМЕНЁННОЙ редакции |
| Бейдж над карточкой в СМЕШАННОЙ сессии | состав сессии проверен по API (4 пары в одном потоке) | ⚠️ **code-only** |
| Справочная коллекция (zh/ja) на экране | `reference_collection_test` (4 кейса) | ⚠️ **code-only; такой коллекции в дев-базе НЕТ** |
| `word_bank` с «Не помню» | `word_bank_give_up_test` (4) + состав свободной практики по API | ⚠️ **code-only** |
| Флаги `pl`/`it`/`ru` в пикере | widget-тест `MiniFlag` | ⚠️ **code-only** |

**Прогон A-4 не закончен, и это надо знать.** Панель симулятора в Claude Code отключена флагом
раскатки ЦЕЛИКОМ — недоступен не только `screenshot`, но и весь канал ввода; assistive-доступ для
`osascript` тоже закрыт, автоматически тапать нечем. Прогон вёлся руками владельца, дошёл до полки
и шапки коллекции — и на этих кадрах бейдж был отменён и переделан, поэтому кадры устарели и
удалены. Каталога `mobile/docs/shots/a4/` НЕТ намеренно: пустой лучше, чем врущий.

## Не переигрывать молча (решения)

Полный список — `docs/DECISIONS.md`. Здесь только то, что легко переиграть по незнанию:

- **Справочность коллекции ВЫВОДИТСЯ, не хранится** (пп. 136, 148): `LanguageRoles::isReference()`
  = справочник знает язык, но `LanguageModeSupport` не даёт ему ни одного тренажёра. Появится судья
  для китайского — ответ поменяется сам. Колонку `is_reference` в БД не заводить.
- **Бейдж пары — флаги 12 px в плашке `faintInk`** (п. 148). Две редакции уже отменены владельцем
  с экрана: кодами («EN→ES») и голыми флагами 15. Правило 14 в `tokens.html` УТОЧНЕНО, а не
  нарушено. Тест пинит плашку — если он мешает, это не повод его снять.
- **`taught_side` — необязательный.** Тай-брейк по профилю (п. 147) остаётся рабочим путём для
  запроса, который роли не назвал; удалять его нельзя, `GET /search` зовут и с экранов без пилюли.
- **`/triage/batch` отвечает `rejected` отдельно от `unknown`.** Это разные факты: `unknown` уходит
  сам, `rejected` не «сядет» никогда, сколько ни повторяй.
- **Разрез `/study/progress` — по ЯЗЫКУ ТЕРМИНА, не по паре** (п. 139). Разрез по паре посчитал бы
  слово из двух папок дважды.
- **`openapi.yaml` до 24.08 не парсился как YAML** и ворота этого не ловили. Починено попутно
  (три инлайн-строки). Если добавляете инлайн-описание с запятой — кавычьте.

## Найдено попутно — в работу НЕ бралось

- **Карточка стора не знает про `is_reference`.** Фид стора (`/store/collections`) флага не несёт,
  поэтому справочная витринная коллекция показала бы пару флагов вместо метки «справочник». Сегодня
  недостижимо (витринных zh/ja нет), но при первой же такой коллекции станет враньём на экране.
- **Горизонтальная карусель «My collections» на главном экране бейджа не получила** — наряд просил
  три поверхности (полка, стор, шапка), это четвёртая.
- **`docs/product/shots/prd-1/` лежит неотслеженным** (два кадра логина) — не из этого трека, в
  коммиты не брался.
- **Английская подпись фазы сессии показывает «Meeting»** на кадре 18:25, хотя п. 49 требует
  «First look». Проверено не было — возможно, кадр со старой сборки; стоит глянуть.
- **`terms.pos` пуст** — единственный оставшийся пункт «Спорного», нужны данные, а не решение.

## Известные ограничения / отложенное

- **П. 138 не реализован**: `term_examples.lang` и отдельная таблица переводов примеров. Принято
  24.08, код не пошёл. У термина с переводами на ru И uk перевод примера ровно один, неизвестно чей.
- `LanguagePurity` знает только ru/uk/en — вне этих языков барьер лукапа честно молчит (п. 144).
- Витринные коллекции несут переводы только `ru`; в паре с другим языком поддержки готовый перевод
  не показывается (п. 146) и его договаривает лукап. Это ограничение контента, не дефект.
- (carried) fork стор-коллекции (B5), лог-ретеншн, коллизия `client_seq` двух устройств
  (принята до TestFlight, п. 140), вывод старого `../backend`.
- Перегенерация витрины ($8.81 верхней границей) — решение заказчика, мораторий не снят.

## Дев-база: что изменено этим нарядом

Бэкап снят до правок (`storage/db-backups/wordtrainer-20260824-190254.sql.gz`).

- В коллекцию QA-аккаунта «Praca po polsku» добавлены два польских слова (`praca`, `biuro`) и
  зачислены в пул — через обычный API, чтобы у смешанной сессии была пара `pl←ru`. Это данные
  QA-аккаунта, не контент витрины.
- Пер-юзерный оверрайд матрицы тренажёров ставился и **снят**: `user_id IS NOT NULL` → 0 строк,
  глобальных 11 — те же, что были (п. 110).

## Running / verifying

- backend2 в Docker (`wt_app` :8001, `wt_db` :5433, `wt_horizon`, `wt_redis`, `wt_ngrok`,
  `wt_admin` :5175). Ворота: commit-hook `composer check`; `composer test` через paratest (~40с
  заявленных, по факту ~245с на этой машине), `composer test-serial` — для отладки.
- mobile: `flutter analyze` + `flutter test` (888). Сборка на телефон — рецепт в `mobile/CLAUDE.md`.
- Симулятор: панель Claude Code может быть выключена целиком (см. `PLAYBOOK` прил. В). Кадры —
  `xcrun simctl io booted screenshot`, ввод — руками владельца.
- Бэкап перед любой миграцией/бэкфиллом: `scripts/db-backup.sh`. `migrate:fresh` на `wordtrainer`
  запрещён всегда — откат проверяется на `wordtrainer_test`.

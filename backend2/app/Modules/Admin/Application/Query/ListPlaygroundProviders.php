<?php

declare(strict_types=1);

namespace App\Modules\Admin\Application\Query;

/** The sandbox picker's contents. Takes nothing — the registry is config, not a filtered listing. */
final readonly class ListPlaygroundProviders {}

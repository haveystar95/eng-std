Before you answer, read each candidate back once: the sentence is wrong on its own, `error_span`
occurs verbatim inside that sentence, and `correction` put in its place reproduces the given example.
A candidate that fails any of the three is fixed or dropped — a set of two that all hold is worth
more than five with a passenger.

Respond with JSON only, matching the provided schema exactly. Do not add commentary.

An item that fails any check is FIXED, not shipped. If you cannot fix it, replace it with a different
one that passes everything — a set where every item passes is worth more than one where the count was
reached by shipping a bad card. Answering in the wrong language is the single most damaging failure
here, because it is what the learner reads as the question and it looks like content rather than like
an error.

Respond with JSON only, matching the provided schema exactly. Do not add commentary.

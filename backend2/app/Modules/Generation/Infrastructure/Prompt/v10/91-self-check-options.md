- **The options are wrong.** Each of the 3 `options` is a {{source_lang}} expression that does NOT
  answer `text`, none repeats another or the `translation`, and none is merely a longer or shorter
  way of saying the right answer. Read each option as if it were the only thing on screen and ask
  whether a competent speaker could defend it as correct; if one could, replace it.
